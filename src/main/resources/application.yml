spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    username: root
    url: jdbc:mysql://localhost:3306/java0725?serverTimezone=UTC
  thymeleaf:
    prefix: classpath:/templates/pages/
    suffix: .html
mybatis-plus:
  config-location: classpath:mybatis-config.xml
  type-aliases-package: com.hw1.pojo
  mapper-locations: classpath:/mapper/*Mapper.xml
